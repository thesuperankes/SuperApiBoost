export const enum TypeFiles{
    START = 1,
    GENERATE = 2
}

export const enum TypeGenerate{
    SCHEMA,
    ROUTE,
    NOSCHEMA,
    ROUTESIMPLE
}

export const enum TypeStart{
    SOMETHING = 1
}